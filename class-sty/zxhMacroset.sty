%----------------------------------------
%---------------zxhMacroset--------------
\ProvidesPackage{zxhMacroset}

%----------------声名图片位置-------------
%----------------对齐方式----------------
%----------------页面设置----------------
%----------------版面设置----------------
%---------------页眉页脚设置--------------
%----------------超链接设置---------------
%----------------计数器设置---------------

%-------------声名图片位置-----------
\graphicspath{{figures/}}

%--------------对齐方式--------------
\raggedbottom   %向上对齐
%\flushbottom    %分散对齐

%--------------页面设置--------------

\linespread{1.5}             %全局修改行距 
%\setlength{\parindent}{10pt} %设置首行缩进
%\setlength{\parskip}{1ex}    %段落之间垂直距离 也可以是一个范围

\setlength{\fboxrule}{0.6pt}  %\fbox 或\framebox 等带框盒子的 线宽
\setlength{\fboxsep}{3pt}     %\fbox 或\framebox 等带框盒子的 内边距
%----------------版面设置----------------
\geometry
{
	a4paper,
	%Word常用格式
	left=1.25in,
	right=1.25in,
	top=1in,
	bottom=1in
	% hmargin=1.25in,vmargin=1in  or margin={1.25in,1in} 左右相同 上下相同
	% margin=1.25in 上下左右边距相同
	% 直接设置body大小 例如：body={8in,11in}
	%单独设置某页格式 使用命令：\thispagestyle{} empty plain headings myheadings
	%缺省\maketitle 命令自动设置其所在页格式为 plain 因此 若想在该页使用 fancy 格式则应该在 \maketile 后面使用命令 \thispagestyle{fancy}
}

%----------------页眉页脚设置----------------
\pagestyle{fancy} %and empty plain headings(article default) myheadings  
%fancy默认格式：
%<head> l{subsection} c{}         r{section} 
%<foot> l{}           c{thepage}  r{}        
%\fancyhf{} % 清空当前的页眉页脚
%\lhead{}   %也可以使用\fancyhead[]{text}
%\chead{}   %---------\fancyfoot[]{text}  
%\rhead{}   
%\lfoot{}    % 内容可为文字 可控制字体
%\cfoot{}    % 若显示页码:\thepage
%\rfoot{}    
\renewcommand{\headrulewidth}{0pt} %页眉页脚线宽
\renewcommand{\footrulewidth}{0pt}

%----------------超链接设置----------------
\hypersetup
{
	colorlinks=true,%or false
	%	pdfborder={0 0 0}
}

%----------------计数器设置----------------
%修改输出格式

%\renewcommand{\thepage}{--~\Roman{page}~--}      %\pagenumbering{Roman}
%\renewcommand{\thefootnote}{[\arabic{footnote}]}
%\renewcommand{\thesection}{\arabic{section}}
%\renewcommand{\thesubsection}{\thesection.\arabic{subsection}} 
%\renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}
%%---对其他计数器重新定义格式方法一致 chapter table figure equation等---
%%---\arabic 可以替换为 \roman \Roman \chinese 等---


\setcounter{secnumdepth}{3} %控制章节编号深度 默认3--subsubsection
\setcounter{tocdepth}{3}    %控制生成目录深度 默认3--subsubsection


