%----------------------------------------
%----------------zxhMacro----------------

\ProvidesPackage{zxhMacro}

%----------------------------------------
%-----------\RequirePackage[]{}----------

\RequirePackage{graphicx} %graphicx宏包辅助支持插入图片
\RequirePackage{multirow} %纵向合并单元格需要用到multirow宏包提供的\multirow命令
\RequirePackage{array}    %array宏包提供了辅助格式>和<，用于给列格式前后加上修饰命令
\RequirePackage{float}    %float宏包为浮动体提供了H 位置参数，不与htbp及!混用
\RequirePackage{booktabs} %三线表由booktabs宏包支持 \toprule \midrule和\bottomrule
\RequirePackage{fancyhdr} %fancyhdr 宏包改善了页眉页脚样式的定义方式
\RequirePackage{geometry} %改变页面布局
\RequirePackage{xcolor}   %rgb cmyk gray hs 自定义颜色-> \definecolor
\RequirePackage{amsmath}  %math sign
\RequirePackage{amsthm}   %Theorem Proof
\RequirePackage{subfig}   %子图小标题 \subfloat[小标题名称]{\includegraphics[]{文件名}}
\RequirePackage{hyperref} %超链接 宏包调用放最后

%----------------------------------------
%----------------\newcommand-------------


%----------------------------------------
%--------------\renewcommand-------------

%------------------标题------------------
%------也可以使用CTEXoptions 设置---------
%\renewcommand{\chaptername}{Chapter}  %\chapter 命令生成的标题前缀
%\renewcommand{\abstractname}{摘要}    %摘要环境abstract 的标题名称
%\renewcommand{\contentsname}{目录}    %\tableofcontents 命令生成的目录标题
%\renewcommand{\indexname}{Index}      %\printindex 命令生成的索引标题
%\renewcommand{\tablename}{Table}      %table 浮动体中\caption 命令生成的标题前缀
%\renewcommand{\figurename}{Figure}    %figure 浮动体中\caption 命令生成的标题前缀
%\renewcommand{\listfigurename}{List of Figures}  %\listoffigures 命令生成的插图目录标题
%\renewcommand{\listtablename}{List of Tables}    %\listoftables 命令生成的表格目录标题
%\renewcommand{\appendixname}{Appendix} %使用\appendix 命令生成的附录部分的章标题前缀
%\renewcommand{\refname}{References} %thebibliography环境或\bibliography 令生成的参考文献标题（article 文档类）

%------------------尺寸------------------  
%\renewcommand{\arraycolsep}{5pt}       %array 环境的表格项前后的间距
%\renewcommand{\tabcolsep}{6pt}         %tabular 环境的表格项前后的间距
%\renewcommand{\arrayrulewidth}{0.4pt}  %表格线宽
%\renewcommand{\doublerulesep}{2pt}     %连续两根表格线之间的间距
%\renewcommand{\abovecaptionskip}{10pt} %\caption 命令位于图表下方时，与上方图表的间距
%\renewcommand{\belowcaptionskip}{0pt}  %\caption 命令位于图表上方时，与下方图表的间距
%\renewcommand{\columnsep}{10pt}        %双栏排版下两栏的间距
%\renewcommand{\columnseprule}{0pt}     %双栏排版下两栏之间竖线的宽度

%----------------------------------------
%-------------\newenvironment------------



